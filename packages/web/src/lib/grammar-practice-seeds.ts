/**
 * @deprecated Prefer practice-seeds.ts (findPracticeSeed).
 * Kept so any old imports keep working.
 */
export { findPracticeSeed as findGrammarSeed, PRACTICE_SEEDS as GRAMMAR_PRACTICE_SEEDS } from "./practice-seeds";
